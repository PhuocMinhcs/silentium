{
  "name": "silentium",
  "version": "1.0.0",
  "description": "Silentium Test",
  "main": "index.js",
  "scripts": {
    "install-client": "cd ./client & yarn install",
    "install-server": "yarn install",
    "run-install": "yarn install-server & yarn install-client",
    "start-client": "cd ./client & yarn start",
    "start-server": "nodemon index",
    "start": "concurrently \"yarn start-client\" \"yarn start-server\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Leesin",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^6.1.0",
    "express": "^4.17.1",
    "nodemon": "^2.0.7"
  }
}
